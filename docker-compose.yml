version: "3.3"
services:
  rtmp:
    build: ./rtmp
    ports:
      - "1935:1935"
      - "80:8080"
    container_name: rtmp_server
    volumes:
      - ./data:/tmp/hls
    links:
      - php

  auth:
    build: ./auth
    container_name: auth_server

  php:
    image: php:7-fpm
    volumes:
      - ./data:/tmp/hls